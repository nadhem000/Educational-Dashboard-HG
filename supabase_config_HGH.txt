============================================================
SUPABASE CONFIGURATION – History & Geography Hub (HGH)
Last updated: 2026-07-23 (CORS + error reporting, working)
============================================================

=======================
1. DATABASE TABLES
=======================

----------
1a. HGH Interaction tracking (separate from dashboard)
----------
create table if not exists hgh_interaction_logs (
  id bigint generated by default as identity primary key,
  entity_type text not null,
  entity_id text not null,
  created_at timestamptz default now()
);

create table if not exists hgh_interaction_counts (
  id bigint generated by default as identity primary key,
  entity_type text not null,
  entity_id text not null,
  count integer default 0 not null,
  unique (entity_type, entity_id)
);

alter table hgh_interaction_logs enable row level security;
alter table hgh_interaction_counts enable row level security;

-- Allow public to read counts (if needed for a future dashboard)
create policy "Allow anon to read HGH counts"
on hgh_interaction_counts
for select
to anon
using (true);

----------
1b. HGH Push notifications tables
----------
create table if not exists hgh_push_subscriptions (
  id bigint generated by default as identity primary key,
  subscription jsonb not null,
  created_at timestamp with time zone default now()
);

create unique index if not exists hgh_push_subscriptions_endpoint_idx
on hgh_push_subscriptions ((subscription->>'endpoint'));

alter table hgh_push_subscriptions enable row level security;

create policy "Allow public insert into HGH subscriptions"
on hgh_push_subscriptions
for insert
with check (true);

----------
1c. HGH Custom notifications table (webhook-driven)
----------
create table if not exists hgh_custom_notifications (
  id        bigint generated by default as identity primary key,
  title     text not null,
  body      text not null,
  created_at timestamptz default now()
);

alter table hgh_custom_notifications enable row level security;

create policy "Allow insert into HGH custom notifications"
  on hgh_custom_notifications
  for insert
  with check (true);

----------
1d. HGH App version (to trigger update push)
----------
create table if not exists hgh_app_version (
  id int primary key default 1,
  version text not null default 'v1',
  check (id = 1)
);

insert into hgh_app_version (id, version)
values (1, 'v1')
on conflict (id) do nothing;

alter table hgh_app_version enable row level security;

create policy "Only service_role can update HGH version"
on hgh_app_version
for update
using (auth.role() = 'service_role');


=======================
2. EDGE FUNCTIONS (HGH)
=======================

----------
2a. log-hgh-interaction (analytics counter + dedup)
----------
URL: https://hmjbzzuresgzwzefjpyt.supabase.co/functions/v1/log-hgh-interaction
JWT verification: OFF (public)

Function code:

import { createClient } from "https://esm.sh/@supabase/supabase-js@2";

const supabaseUrl = Deno.env.get("SUPABASE_URL")!;
const supabaseKey = Deno.env.get("SUPABASE_SERVICE_ROLE_KEY")!;
const supabase = createClient(supabaseUrl, supabaseKey);

const DEDUP_WINDOW_MS = 10000;

const corsHeaders = {
  "Access-Control-Allow-Origin": "*",
  "Access-Control-Allow-Methods": "POST, OPTIONS",
  "Access-Control-Allow-Headers": "Content-Type, Authorization",
  "Access-Control-Max-Age": "86400",
};

Deno.serve(async (req) => {
  if (req.method === "OPTIONS") {
    return new Response(null, { status: 204, headers: corsHeaders });
  }
  if (req.method !== "POST") {
    return new Response("Method not allowed", { status: 405, headers: corsHeaders });
  }

  let body: { entity_type?: string; entity_id?: string };
  try {
    body = await req.json();
  } catch {
    return new Response("Invalid JSON", { status: 400, headers: corsHeaders });
  }

  const { entity_type, entity_id } = body;
  if (!entity_type || !entity_id) {
    return new Response("Missing fields", { status: 400, headers: corsHeaders });
  }

  // Dedup check (HGH tables)
  const { data: lastEntry, error: selectError } = await supabase
    .from("hgh_interaction_logs")
    .select("created_at")
    .eq("entity_type", entity_type)
    .eq("entity_id", entity_id)
    .order("created_at", { ascending: false })
    .limit(1)
    .maybeSingle();

  if (selectError) {
    return new Response(`Select error: ${selectError.message}`, { status: 500, headers: corsHeaders });
  }
  if (lastEntry) {
    const lastTime = new Date(lastEntry.created_at).getTime();
    if (Date.now() - lastTime < DEDUP_WINDOW_MS) {
      return new Response("Too many requests", { status: 429, headers: corsHeaders });
    }
  }

  // Insert log
  const { error: insertLogError } = await supabase
    .from("hgh_interaction_logs")
    .insert({ entity_type, entity_id });
  if (insertLogError) {
    return new Response(`Insert log error: ${insertLogError.message}`, { status: 500, headers: corsHeaders });
  }

  // Upsert count
  const { data: currentRow, error: readError } = await supabase
    .from("hgh_interaction_counts")
    .select("count")
    .eq("entity_type", entity_type)
    .eq("entity_id", entity_id)
    .maybeSingle();
  if (readError) {
    return new Response(`Read count error: ${readError.message}`, { status: 500, headers: corsHeaders });
  }

  const newCount = (currentRow?.count ?? 0) + 1;
  const { error: upsertError } = await supabase
    .from("hgh_interaction_counts")
    .upsert(
      { entity_type, entity_id, count: newCount },
      { onConflict: "entity_type, entity_id" }
    );
  if (upsertError) {
    return new Response(`Upsert error: ${upsertError.message}`, { status: 500, headers: corsHeaders });
  }

  return new Response("OK", { status: 200, headers: corsHeaders });
});


----------
2b. send-hgh-version-push (push for version updates) – WORKING VERSION
----------
URL: https://hmjbzzuresgzwzefjpyt.supabase.co/functions/v1/send-hgh-version-push
Environment variable: WEBHOOK_SECRET = nadhem000123

Function code (includes CORS + detailed error reporting):

import { createClient } from "https://esm.sh/@supabase/supabase-js@2";
import webpush from "npm:web-push";

const SUPABASE_URL = Deno.env.get("SUPABASE_URL")!;
const SUPABASE_SERVICE_KEY = Deno.env.get("SUPABASE_SERVICE_ROLE_KEY")!;
const WEBHOOK_SECRET = Deno.env.get("WEBHOOK_SECRET")!;

webpush.setVapidDetails(
  "mailto:mejri.ziad@gmail.com",
  "BO2xV3C8PcVFX-2SqsAyHZgzyDBaC0N7GzDktpoX2J5Pj5Cz9IazAcybQqxe13xYqGnEpoVmXM2jHFqmjXko1kw",
  "1tRRT8P1I9GdnV6oAJ8zXm1Fe4Dd_KrYx-TXikjAJz4"
);

const corsHeaders = {
  "Access-Control-Allow-Origin": "*",
  "Access-Control-Allow-Methods": "POST, OPTIONS",
  "Access-Control-Allow-Headers": "Content-Type, Authorization",
  "Access-Control-Max-Age": "86400",
};

Deno.serve(async (req) => {
  if (req.method === "OPTIONS") {
    return new Response(null, { status: 204, headers: corsHeaders });
  }
  if (req.method !== "POST") {
    return new Response("Method not allowed", { status: 405, headers: corsHeaders });
  }
  const authHeader = req.headers.get("authorization");
  if (authHeader !== `Bearer ${WEBHOOK_SECRET}`) {
    return new Response("Unauthorized", { status: 401, headers: corsHeaders });
  }

  try {
    const supabase = createClient(SUPABASE_URL, SUPABASE_SERVICE_KEY);
    const { data: subs, error } = await supabase
      .from("hgh_push_subscriptions")
      .select("subscription");
    if (error) throw new Error(`Supabase error: ${error.message}`);
    if (!subs || subs.length === 0) {
      return new Response("No subscriptions", { status: 200, headers: corsHeaders });
    }

    const payload = JSON.stringify({
      title: "HGH Updated",
      body: "A new version of History & Geography Hub is ready.",
    });

    const results = await Promise.allSettled(
      subs.map(async ({ subscription }) => {
        if (!subscription || !subscription.endpoint) {
          throw new Error("Invalid subscription object");
        }
        return await webpush.sendNotification(subscription, payload);
      })
    );

    const errors = results
      .filter(r => r.status === "rejected")
      .map(r => {
        const e = r.reason;
        return `Error: ${e.message}, statusCode: ${e.statusCode}, body: ${JSON.stringify(e.body)}`;
      });

    const succeeded = results.filter(r => r.status === "fulfilled").length;
    const failed = results.filter(r => r.status === "rejected").length;

    return new Response(
      `Sent ${succeeded}/${results.length}, failed ${failed}` +
      (errors.length ? ` | Errors: ${errors.join("; ")}` : ""),
      { status: 200, headers: corsHeaders }
    );
  } catch (err) {
    const errorMessage = err instanceof Error ? err.message : String(err);
    console.error(errorMessage);
    return new Response(`Error: ${errorMessage}`, { status: 500, headers: corsHeaders });
  }
});


----------
2c. send-hgh-custom-push (custom notification text) – WORKING VERSION
----------
URL: https://hmjbzzuresgzwzefjpyt.supabase.co/functions/v1/send-hgh-custom-push
Environment variable: WEBHOOK_SECRET = nadhem000123

Function code (includes CORS + detailed error reporting):

import { createClient } from "https://esm.sh/@supabase/supabase-js@2";
import webpush from "npm:web-push";

const SUPABASE_URL = Deno.env.get("SUPABASE_URL")!;
const SUPABASE_SERVICE_KEY = Deno.env.get("SUPABASE_SERVICE_ROLE_KEY")!;
const WEBHOOK_SECRET = Deno.env.get("WEBHOOK_SECRET")!;

webpush.setVapidDetails(
  "mailto:mejri.ziad@gmail.com",
  "BO2xV3C8PcVFX-2SqsAyHZgzyDBaC0N7GzDktpoX2J5Pj5Cz9IazAcybQqxe13xYqGnEpoVmXM2jHFqmjXko1kw",
  "1tRRT8P1I9GdnV6oAJ8zXm1Fe4Dd_KrYx-TXikjAJz4"
);

const corsHeaders = {
  "Access-Control-Allow-Origin": "*",
  "Access-Control-Allow-Methods": "POST, OPTIONS",
  "Access-Control-Allow-Headers": "Content-Type, Authorization",
  "Access-Control-Max-Age": "86400",
};

Deno.serve(async (req) => {
  if (req.method === "OPTIONS") {
    return new Response(null, { status: 204, headers: corsHeaders });
  }
  if (req.method !== "POST") {
    return new Response("Method not allowed", { status: 405, headers: corsHeaders });
  }

  const authHeader = req.headers.get("authorization");
  if (authHeader !== `Bearer ${WEBHOOK_SECRET}`) {
    return new Response("Unauthorized", { status: 401, headers: corsHeaders });
  }

  try {
    // Webhook payload contains the newly inserted row
    const payload = await req.json();
    const title = payload?.record?.title;
    const body = payload?.record?.body;

    if (!title || !body) {
      return new Response("Missing title or body", { status: 400, headers: corsHeaders });
    }

    const supabase = createClient(SUPABASE_URL, SUPABASE_SERVICE_KEY);
    const { data: subs, error } = await supabase
      .from("hgh_push_subscriptions")
      .select("subscription");

    if (error) throw new Error(`Supabase error: ${error.message}`);
    if (!subs || subs.length === 0) {
      return new Response("No subscriptions", { status: 200, headers: corsHeaders });
    }

    const notificationPayload = JSON.stringify({ title, body });
    const results = await Promise.allSettled(
      subs.map(async ({ subscription }) => {
        if (!subscription || !subscription.endpoint) {
          throw new Error("Invalid subscription object");
        }
        return await webpush.sendNotification(subscription, notificationPayload);
      })
    );

    const errors = results
      .filter(r => r.status === "rejected")
      .map(r => {
        const e = r.reason;
        return `Error: ${e.message}, statusCode: ${e.statusCode}, body: ${JSON.stringify(e.body)}`;
      });

    const succeeded = results.filter(r => r.status === "fulfilled").length;
    const failed = results.filter(r => r.status === "rejected").length;

    return new Response(
      `Sent ${succeeded}/${results.length}, failed ${failed}` +
      (errors.length ? ` | Errors: ${errors.join("; ")}` : ""),
      { status: 200, headers: corsHeaders }
    );
  } catch (err) {
    const errorMessage = err instanceof Error ? err.message : String(err);
    console.error("Function error:", errorMessage);
    return new Response(`Error: ${errorMessage}`, { status: 500, headers: corsHeaders });
  }
});


=======================
3. DATABASE WEBHOOKS
=======================

----------
3a. hgh_version_update_notify
----------
Name: hgh_version_update_notify
Table: hgh_app_version
Event: UPDATE
HTTP Method: POST
URL: https://hmjbzzuresgzwzefjpyt.supabase.co/functions/v1/send-hgh-version-push
Headers:
  Authorization: Bearer nadhem000123

----------
3b. hgh_custom_push_notification
----------
Name: hgh_custom_push_notification
Table: hgh_custom_notifications
Event: INSERT
HTTP Method: POST
URL: https://hmjbzzuresgzwzefjpyt.supabase.co/functions/v1/send-hgh-custom-push
Headers:
  Authorization: Bearer nadhem000123


=======================
4. AUTHENTICATION CONFIG (shared with dashboard)
=======================
- Supabase project: https://hmjbzzuresgzwzefjpyt.supabase.co
- Email provider enabled.
- Site URL for HGH: https://educational-dashboard-hg.netlify.app (update if domain changes)
- Redirect URLs: add your HGH domain + /reset-password.html
- Confirm email in sign‑up: disabled (same as dashboard)


=======================
5. QUICK SQL SCRIPTS (useful for maintenance)
=======================

-- 5a. Send a custom push notification
INSERT INTO hgh_custom_notifications (title, body)
VALUES (
  '🚀 New Lesson Available!',
  'Check out the new chapter on the French Revolution.'
);
-- (the webhook will automatically dispatch it)

-- 5b. Bump app version (triggers version push)
UPDATE hgh_app_version SET version = 'v2' WHERE id = 1;

-- 5c. View recent interactions (last 100 logs)
SELECT * FROM hgh_interaction_logs ORDER BY created_at DESC LIMIT 100;

-- 5d. Top 10 most interacted entities
SELECT entity_type, entity_id, count(*) as total
FROM hgh_interaction_logs
GROUP BY entity_type, entity_id
ORDER BY total DESC
LIMIT 10;

-- 5e. Count total push subscriptions
SELECT count(*) FROM hgh_push_subscriptions;

-- 5f. Remove duplicate push subscriptions (by endpoint)
DELETE FROM hgh_push_subscriptions
WHERE id NOT IN (
  SELECT min(id)
  FROM hgh_push_subscriptions
  GROUP BY subscription->>'endpoint'
);

-- 5g. Clean all subscriptions and start fresh (if push stops working)
DELETE FROM hgh_push_subscriptions;
-- Then unregister service worker in browser, reload, and grant notification permission again.

-- 5h. See current app rating submissions (if you implement rating later)
SELECT entity_id as rating, count(*) as number_of_votes
FROM hgh_interaction_logs
WHERE entity_type = 'app_rating'
GROUP BY entity_id
ORDER BY rating::int;